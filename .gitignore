# Virtual environment
venv/
.venv/

# Python cache
__pycache__/
*.pyc

# Cached parsed data (regenerates automatically, don't commit it)
data/projects_cache.json

# Environment variables / secrets — NEVER commit your API key
.env

# Uploaded user files (resume, projects doc) — personal, don't commit
data/*.docx
data/*.pdf

# OS junk
.DS_Store